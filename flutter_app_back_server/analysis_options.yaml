# Lint rules live in the shared config at the repo root.
include: ../analysis_options.yaml

analyzer:
  exclude:
    - lib/src/generated/**
    - migrations/**
    - test/integration/test_tools/serverpod_test_tools.dart
